// Template file for environment.ts
// Copy this to environment.ts and configure your settings

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api'
  // Note: Credentials are stored securely on the backend in .env file
  // Frontend no longer needs to know about Service Principal credentials
};

